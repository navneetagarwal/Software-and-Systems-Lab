Citations:
 Stackoverflow
 Python Documentation 


First question:
The git log is in remoterepo/log.txt. If we push before we pull, the git initialises an error as the pull command hasn't been completed initially to sync the errors

Second Question
The input requires 2 argument, and the parts are labelled accordingly. THe output is in the file is marks_sorted.csv

Challenge Question
The input file is assumed to be tenth.txt
The output file is cbseProcessed.csv
The code is based only on algorithms which can be observed and no cell value is hardcoded. The case is completely generalized sa far as possible seeing the data values .