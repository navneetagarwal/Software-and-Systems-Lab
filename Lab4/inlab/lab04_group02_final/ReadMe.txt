Citations 
Stack_overflow
Google

Note - We couldn't finish 1 d) problem because we had problems in 1 a) problem.


1)a_)Passphrase is longer and more secure than password
Password are easier to crack and passphrases are easier to remember


1c)Scp basically reads the source file and writes it to the destination. It performs a plain linear copy, locally, or over a network.

Rsync also copies files locally or over a network. But it employs a special delta transfer algorithm and a few optimizations to make the operation a lot faster.

2) COmplexity nlog n
